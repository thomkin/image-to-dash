import { ImageToDash } from "./imageToDash.js";
import type {
  ImageToDashOptions,
  ImageToDashEvents,
  ProcessResult,
} from "./types.js";

export {
  ImageToDash as ImageProcessor,
  type ImageToDashOptions as ImageProcessorOptions,
  type ImageToDashEvents as ImageProcessorEvents,
  type ProcessResult,
};

export default ImageToDash;
